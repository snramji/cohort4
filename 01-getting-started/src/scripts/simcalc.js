// Calculator Functions

const simCalc = {

    add: (num1, num2) => {
        console.log(num1 + num2);
        return num1 + num2;
    },

    subtract: (num1, num2) => {
        console.log(num1 - num2);
        return num1 - num2;
    },

    multiply: (num1, num2) => {
        console.log(num1 * num2);
        return num1 * num2;
    },

    divide: (num1, num2) => {
        console.log(num1 / num2);
        return num1 / num2;
    },

};

export default simCalc;