// Logic for Working With Arrays

let input_Array = [];
const arrays = {     
    
    arrayAdd: (inputArray) => {
        inputArray.push(input_Array);
        console.log(inputArray);
        return inputArray;
    },

};


export default arrays;